package com.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.example.a17916.test4_hook.database.JoinResourceActivity;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "JOIN_RESOURCE_ACTIVITY".
*/
public class JoinResourceActivityDao extends AbstractDao<JoinResourceActivity, Long> {

    public static final String TABLENAME = "JOIN_RESOURCE_ACTIVITY";

    /**
     * Properties of entity JoinResourceActivity.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property ResId = new Property(1, Long.class, "resId", false, "RES_ID");
        public final static Property ActivityId = new Property(2, Long.class, "activityId", false, "ACTIVITY_ID");
    }


    public JoinResourceActivityDao(DaoConfig config) {
        super(config);
    }
    
    public JoinResourceActivityDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"JOIN_RESOURCE_ACTIVITY\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"RES_ID\" INTEGER," + // 1: resId
                "\"ACTIVITY_ID\" INTEGER);"); // 2: activityId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"JOIN_RESOURCE_ACTIVITY\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, JoinResourceActivity entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Long resId = entity.getResId();
        if (resId != null) {
            stmt.bindLong(2, resId);
        }
 
        Long activityId = entity.getActivityId();
        if (activityId != null) {
            stmt.bindLong(3, activityId);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, JoinResourceActivity entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Long resId = entity.getResId();
        if (resId != null) {
            stmt.bindLong(2, resId);
        }
 
        Long activityId = entity.getActivityId();
        if (activityId != null) {
            stmt.bindLong(3, activityId);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public JoinResourceActivity readEntity(Cursor cursor, int offset) {
        JoinResourceActivity entity = new JoinResourceActivity( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1), // resId
            cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2) // activityId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, JoinResourceActivity entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setResId(cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1));
        entity.setActivityId(cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(JoinResourceActivity entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(JoinResourceActivity entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(JoinResourceActivity entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
