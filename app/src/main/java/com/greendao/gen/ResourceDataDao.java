package com.greendao.gen;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.SqlUtils;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;
import org.greenrobot.greendao.query.Query;
import org.greenrobot.greendao.query.QueryBuilder;

import com.example.a17916.test4_hook.database.AppData;

import com.example.a17916.test4_hook.database.ResourceData;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "RESOURCE_DATA".
*/
public class ResourceDataDao extends AbstractDao<ResourceData, Long> {

    public static final String TABLENAME = "RESOURCE_DATA";

    /**
     * Properties of entity ResourceData.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property ResId = new Property(0, Long.class, "resId", true, "_id");
        public final static Property AppId = new Property(1, Long.class, "appId", false, "APP_ID");
        public final static Property ResCategory = new Property(2, String.class, "resCategory", false, "RES_CATEGORY");
        public final static Property ResEntityName = new Property(3, String.class, "resEntityName", false, "RES_ENTITY_NAME");
        public final static Property ActivityId = new Property(4, Long.class, "activityId", false, "ACTIVITY_ID");
    }

    private DaoSession daoSession;

    private Query<ResourceData> activityData_ResourceDatasQuery;
    private Query<ResourceData> appData_ResourceDatasQuery;

    public ResourceDataDao(DaoConfig config) {
        super(config);
    }
    
    public ResourceDataDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"RESOURCE_DATA\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: resId
                "\"APP_ID\" INTEGER," + // 1: appId
                "\"RES_CATEGORY\" TEXT," + // 2: resCategory
                "\"RES_ENTITY_NAME\" TEXT," + // 3: resEntityName
                "\"ACTIVITY_ID\" INTEGER);"); // 4: activityId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"RESOURCE_DATA\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ResourceData entity) {
        stmt.clearBindings();
 
        Long resId = entity.getResId();
        if (resId != null) {
            stmt.bindLong(1, resId);
        }
 
        Long appId = entity.getAppId();
        if (appId != null) {
            stmt.bindLong(2, appId);
        }
 
        String resCategory = entity.getResCategory();
        if (resCategory != null) {
            stmt.bindString(3, resCategory);
        }
 
        String resEntityName = entity.getResEntityName();
        if (resEntityName != null) {
            stmt.bindString(4, resEntityName);
        }
 
        Long activityId = entity.getActivityId();
        if (activityId != null) {
            stmt.bindLong(5, activityId);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ResourceData entity) {
        stmt.clearBindings();
 
        Long resId = entity.getResId();
        if (resId != null) {
            stmt.bindLong(1, resId);
        }
 
        Long appId = entity.getAppId();
        if (appId != null) {
            stmt.bindLong(2, appId);
        }
 
        String resCategory = entity.getResCategory();
        if (resCategory != null) {
            stmt.bindString(3, resCategory);
        }
 
        String resEntityName = entity.getResEntityName();
        if (resEntityName != null) {
            stmt.bindString(4, resEntityName);
        }
 
        Long activityId = entity.getActivityId();
        if (activityId != null) {
            stmt.bindLong(5, activityId);
        }
    }

    @Override
    protected final void attachEntity(ResourceData entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public ResourceData readEntity(Cursor cursor, int offset) {
        ResourceData entity = new ResourceData( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // resId
            cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1), // appId
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // resCategory
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // resEntityName
            cursor.isNull(offset + 4) ? null : cursor.getLong(offset + 4) // activityId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ResourceData entity, int offset) {
        entity.setResId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setAppId(cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1));
        entity.setResCategory(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setResEntityName(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setActivityId(cursor.isNull(offset + 4) ? null : cursor.getLong(offset + 4));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(ResourceData entity, long rowId) {
        entity.setResId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(ResourceData entity) {
        if(entity != null) {
            return entity.getResId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(ResourceData entity) {
        return entity.getResId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "resourceDatas" to-many relationship of ActivityData. */
    public List<ResourceData> _queryActivityData_ResourceDatas(Long resId) {
        synchronized (this) {
            if (activityData_ResourceDatasQuery == null) {
                QueryBuilder<ResourceData> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.ResId.eq(null));
                activityData_ResourceDatasQuery = queryBuilder.build();
            }
        }
        Query<ResourceData> query = activityData_ResourceDatasQuery.forCurrentThread();
        query.setParameter(0, resId);
        return query.list();
    }

    /** Internal query to resolve the "resourceDatas" to-many relationship of AppData. */
    public List<ResourceData> _queryAppData_ResourceDatas(Long resId) {
        synchronized (this) {
            if (appData_ResourceDatasQuery == null) {
                QueryBuilder<ResourceData> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.ResId.eq(null));
                appData_ResourceDatasQuery = queryBuilder.build();
            }
        }
        Query<ResourceData> query = appData_ResourceDatasQuery.forCurrentThread();
        query.setParameter(0, resId);
        return query.list();
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getAppDataDao().getAllColumns());
            builder.append(" FROM RESOURCE_DATA T");
            builder.append(" LEFT JOIN APP_DATA T0 ON T.\"APP_ID\"=T0.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected ResourceData loadCurrentDeep(Cursor cursor, boolean lock) {
        ResourceData entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        AppData app = loadCurrentOther(daoSession.getAppDataDao(), cursor, offset);
        entity.setApp(app);

        return entity;    
    }

    public ResourceData loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<ResourceData> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<ResourceData> list = new ArrayList<ResourceData>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<ResourceData> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<ResourceData> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
