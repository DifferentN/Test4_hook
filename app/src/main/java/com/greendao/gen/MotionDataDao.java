package com.greendao.gen;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.SqlUtils;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.example.a17916.test4_hook.database.ActivityData;

import com.example.a17916.test4_hook.database.MotionData;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "MOTION_DATA".
*/
public class MotionDataDao extends AbstractDao<MotionData, Long> {

    public static final String TABLENAME = "MOTION_DATA";

    /**
     * Properties of entity MotionData.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property ActivityName = new Property(1, String.class, "activityName", false, "ActivityName");
        public final static Property MotionSeq = new Property(2, Long.class, "motionSeq", false, "MOTION_SEQ");
        public final static Property ActivityId = new Property(3, Long.class, "activityId", false, "ACTIVITY_ID");
        public final static Property ResCategory = new Property(4, String.class, "resCategory", false, "RES_CATEGORY");
        public final static Property ContentKey = new Property(5, String.class, "contentKey", false, "ContentKey");
        public final static Property Bytes = new Property(6, byte[].class, "bytes", false, "EventsByte");
    }

    private DaoSession daoSession;


    public MotionDataDao(DaoConfig config) {
        super(config);
    }
    
    public MotionDataDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"MOTION_DATA\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"ActivityName\" TEXT," + // 1: activityName
                "\"MOTION_SEQ\" INTEGER," + // 2: motionSeq
                "\"ACTIVITY_ID\" INTEGER," + // 3: activityId
                "\"RES_CATEGORY\" TEXT," + // 4: resCategory
                "\"ContentKey\" TEXT," + // 5: contentKey
                "\"EventsByte\" BLOB);"); // 6: bytes
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"MOTION_DATA\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, MotionData entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String activityName = entity.getActivityName();
        if (activityName != null) {
            stmt.bindString(2, activityName);
        }
 
        Long motionSeq = entity.getMotionSeq();
        if (motionSeq != null) {
            stmt.bindLong(3, motionSeq);
        }
 
        Long activityId = entity.getActivityId();
        if (activityId != null) {
            stmt.bindLong(4, activityId);
        }
 
        String resCategory = entity.getResCategory();
        if (resCategory != null) {
            stmt.bindString(5, resCategory);
        }
 
        String contentKey = entity.getContentKey();
        if (contentKey != null) {
            stmt.bindString(6, contentKey);
        }
 
        byte[] bytes = entity.getBytes();
        if (bytes != null) {
            stmt.bindBlob(7, bytes);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, MotionData entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String activityName = entity.getActivityName();
        if (activityName != null) {
            stmt.bindString(2, activityName);
        }
 
        Long motionSeq = entity.getMotionSeq();
        if (motionSeq != null) {
            stmt.bindLong(3, motionSeq);
        }
 
        Long activityId = entity.getActivityId();
        if (activityId != null) {
            stmt.bindLong(4, activityId);
        }
 
        String resCategory = entity.getResCategory();
        if (resCategory != null) {
            stmt.bindString(5, resCategory);
        }
 
        String contentKey = entity.getContentKey();
        if (contentKey != null) {
            stmt.bindString(6, contentKey);
        }
 
        byte[] bytes = entity.getBytes();
        if (bytes != null) {
            stmt.bindBlob(7, bytes);
        }
    }

    @Override
    protected final void attachEntity(MotionData entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public MotionData readEntity(Cursor cursor, int offset) {
        MotionData entity = new MotionData( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // activityName
            cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2), // motionSeq
            cursor.isNull(offset + 3) ? null : cursor.getLong(offset + 3), // activityId
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // resCategory
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // contentKey
            cursor.isNull(offset + 6) ? null : cursor.getBlob(offset + 6) // bytes
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, MotionData entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setActivityName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setMotionSeq(cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2));
        entity.setActivityId(cursor.isNull(offset + 3) ? null : cursor.getLong(offset + 3));
        entity.setResCategory(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setContentKey(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setBytes(cursor.isNull(offset + 6) ? null : cursor.getBlob(offset + 6));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(MotionData entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(MotionData entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(MotionData entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getActivityDataDao().getAllColumns());
            builder.append(" FROM MOTION_DATA T");
            builder.append(" LEFT JOIN ACTIVITY_DATA T0 ON T.\"ACTIVITY_ID\"=T0.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected MotionData loadCurrentDeep(Cursor cursor, boolean lock) {
        MotionData entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        ActivityData activityData = loadCurrentOther(daoSession.getActivityDataDao(), cursor, offset);
        entity.setActivityData(activityData);

        return entity;    
    }

    public MotionData loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<MotionData> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<MotionData> list = new ArrayList<MotionData>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<MotionData> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<MotionData> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
