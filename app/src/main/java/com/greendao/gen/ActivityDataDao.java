package com.greendao.gen;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.SqlUtils;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;
import org.greenrobot.greendao.query.Query;
import org.greenrobot.greendao.query.QueryBuilder;

import com.example.a17916.test4_hook.database.AppData;

import com.example.a17916.test4_hook.database.ActivityData;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "ACTIVITY_DATA".
*/
public class ActivityDataDao extends AbstractDao<ActivityData, Long> {

    public static final String TABLENAME = "ACTIVITY_DATA";

    /**
     * Properties of entity ActivityData.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property ActivityId = new Property(0, Long.class, "activityId", true, "_id");
        public final static Property ActivityName = new Property(1, String.class, "activityName", false, "ACTIVITY_NAME");
        public final static Property AppId = new Property(2, Long.class, "appId", false, "APP_ID");
        public final static Property ResId = new Property(3, Long.class, "resId", false, "RES_ID");
    }

    private DaoSession daoSession;

    private Query<ActivityData> resourceData_ActivityDataQuery;

    public ActivityDataDao(DaoConfig config) {
        super(config);
    }
    
    public ActivityDataDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"ACTIVITY_DATA\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: activityId
                "\"ACTIVITY_NAME\" TEXT," + // 1: activityName
                "\"APP_ID\" INTEGER," + // 2: appId
                "\"RES_ID\" INTEGER);"); // 3: resId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"ACTIVITY_DATA\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ActivityData entity) {
        stmt.clearBindings();
 
        Long activityId = entity.getActivityId();
        if (activityId != null) {
            stmt.bindLong(1, activityId);
        }
 
        String activityName = entity.getActivityName();
        if (activityName != null) {
            stmt.bindString(2, activityName);
        }
 
        Long appId = entity.getAppId();
        if (appId != null) {
            stmt.bindLong(3, appId);
        }
 
        Long resId = entity.getResId();
        if (resId != null) {
            stmt.bindLong(4, resId);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ActivityData entity) {
        stmt.clearBindings();
 
        Long activityId = entity.getActivityId();
        if (activityId != null) {
            stmt.bindLong(1, activityId);
        }
 
        String activityName = entity.getActivityName();
        if (activityName != null) {
            stmt.bindString(2, activityName);
        }
 
        Long appId = entity.getAppId();
        if (appId != null) {
            stmt.bindLong(3, appId);
        }
 
        Long resId = entity.getResId();
        if (resId != null) {
            stmt.bindLong(4, resId);
        }
    }

    @Override
    protected final void attachEntity(ActivityData entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public ActivityData readEntity(Cursor cursor, int offset) {
        ActivityData entity = new ActivityData( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // activityId
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // activityName
            cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2), // appId
            cursor.isNull(offset + 3) ? null : cursor.getLong(offset + 3) // resId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ActivityData entity, int offset) {
        entity.setActivityId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setActivityName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setAppId(cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2));
        entity.setResId(cursor.isNull(offset + 3) ? null : cursor.getLong(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(ActivityData entity, long rowId) {
        entity.setActivityId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(ActivityData entity) {
        if(entity != null) {
            return entity.getActivityId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(ActivityData entity) {
        return entity.getActivityId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "activityData" to-many relationship of ResourceData. */
    public List<ActivityData> _queryResourceData_ActivityData(Long activityId) {
        synchronized (this) {
            if (resourceData_ActivityDataQuery == null) {
                QueryBuilder<ActivityData> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.ActivityId.eq(null));
                resourceData_ActivityDataQuery = queryBuilder.build();
            }
        }
        Query<ActivityData> query = resourceData_ActivityDataQuery.forCurrentThread();
        query.setParameter(0, activityId);
        return query.list();
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getAppDataDao().getAllColumns());
            builder.append(" FROM ACTIVITY_DATA T");
            builder.append(" LEFT JOIN APP_DATA T0 ON T.\"APP_ID\"=T0.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected ActivityData loadCurrentDeep(Cursor cursor, boolean lock) {
        ActivityData entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        AppData appData = loadCurrentOther(daoSession.getAppDataDao(), cursor, offset);
        entity.setAppData(appData);

        return entity;    
    }

    public ActivityData loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<ActivityData> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<ActivityData> list = new ArrayList<ActivityData>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<ActivityData> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<ActivityData> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
