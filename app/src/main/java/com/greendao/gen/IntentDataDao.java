package com.greendao.gen;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.SqlUtils;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.example.a17916.test4_hook.database.ActivityData;
import com.example.a17916.test4_hook.database.ResourceData;

import com.example.a17916.test4_hook.database.IntentData;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "INTENT_DATA".
*/
public class IntentDataDao extends AbstractDao<IntentData, Long> {

    public static final String TABLENAME = "INTENT_DATA";

    /**
     * Properties of entity IntentData.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property ActivityId = new Property(1, Long.class, "activityId", false, "ACTIVITY_ID");
        public final static Property ResId = new Property(2, Long.class, "resId", false, "RES_ID");
        public final static Property Bytes = new Property(3, byte[].class, "bytes", false, "IntentByte");
    }

    private DaoSession daoSession;


    public IntentDataDao(DaoConfig config) {
        super(config);
    }
    
    public IntentDataDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"INTENT_DATA\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"ACTIVITY_ID\" INTEGER," + // 1: activityId
                "\"RES_ID\" INTEGER," + // 2: resId
                "\"IntentByte\" BLOB);"); // 3: bytes
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"INTENT_DATA\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, IntentData entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Long activityId = entity.getActivityId();
        if (activityId != null) {
            stmt.bindLong(2, activityId);
        }
 
        Long resId = entity.getResId();
        if (resId != null) {
            stmt.bindLong(3, resId);
        }
 
        byte[] bytes = entity.getBytes();
        if (bytes != null) {
            stmt.bindBlob(4, bytes);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, IntentData entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Long activityId = entity.getActivityId();
        if (activityId != null) {
            stmt.bindLong(2, activityId);
        }
 
        Long resId = entity.getResId();
        if (resId != null) {
            stmt.bindLong(3, resId);
        }
 
        byte[] bytes = entity.getBytes();
        if (bytes != null) {
            stmt.bindBlob(4, bytes);
        }
    }

    @Override
    protected final void attachEntity(IntentData entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public IntentData readEntity(Cursor cursor, int offset) {
        IntentData entity = new IntentData( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1), // activityId
            cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2), // resId
            cursor.isNull(offset + 3) ? null : cursor.getBlob(offset + 3) // bytes
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, IntentData entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setActivityId(cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1));
        entity.setResId(cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2));
        entity.setBytes(cursor.isNull(offset + 3) ? null : cursor.getBlob(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(IntentData entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(IntentData entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(IntentData entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getActivityDataDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getResourceDataDao().getAllColumns());
            builder.append(" FROM INTENT_DATA T");
            builder.append(" LEFT JOIN ACTIVITY_DATA T0 ON T.\"ACTIVITY_ID\"=T0.\"_id\"");
            builder.append(" LEFT JOIN RESOURCE_DATA T1 ON T.\"RES_ID\"=T1.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected IntentData loadCurrentDeep(Cursor cursor, boolean lock) {
        IntentData entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        ActivityData activityData = loadCurrentOther(daoSession.getActivityDataDao(), cursor, offset);
        entity.setActivityData(activityData);
        offset += daoSession.getActivityDataDao().getAllColumns().length;

        ResourceData resourceData = loadCurrentOther(daoSession.getResourceDataDao(), cursor, offset);
        entity.setResourceData(resourceData);

        return entity;    
    }

    public IntentData loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<IntentData> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<IntentData> list = new ArrayList<IntentData>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<IntentData> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<IntentData> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
